import { LookupNode } from './lookup'


describe('LookupNode', () => {
    describe('.matchPattern(...)', () => {
        it.todo('returns no matches, given tokens which do not match in any context')
    })
})
