{
  "name": "auto-complete",
  "author": "asmr.hex",
  "license": "ISC",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "main": "dist/auto-complete.umd.js",
  "browser": "dist/auto-complete.umd.js",
  "module": "lib/index.js",
  "unpkg": "dist/auto-complete.umd.js",
  "types": "lib/types/index.d.ts",
  "scripts": {
    "build": "npm run build:lib && npm run build:dist",
    "build:lib": "rm -rf lib && npx tsc",
    "build:dist": "rm -rf dist && npx webpack",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "lib",
    "package.json"
  ],
  "devDependencies": {
    "ts-loader": "^8.0.17",
    "typescript": "^4.1.5",
    "webpack": "^5.22.0",
    "webpack-cli": "^4.5.0"
  }
}
